# Simulator Scene Placeholder Slot

Store optional x86_64 simulator scene snapshot payloads here when A-thread provides them.

Expected naming:

- `scene.x86_64.<type>.<mode>.<scenario>.json`

If the folder contains no matching JSON files, simulator placeholder validation reports `skipped-no-x86-placeholder`.
