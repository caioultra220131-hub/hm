# Recordings Placeholder

Drop the raw trace artifact generated by A-thread recording here.

Constraints for this skeleton:

- file name is open
- file format is open
- payload fields are open

Only the folder location is stable for now.

