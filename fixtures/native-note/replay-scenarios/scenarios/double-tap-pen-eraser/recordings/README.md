# Recording Slot

Drop the real device trace exported for `double_tap_pen_eraser` into this folder.

Keep the original file name and extension from the device export.
Do not normalize or rewrite the raw payload in round 1.
