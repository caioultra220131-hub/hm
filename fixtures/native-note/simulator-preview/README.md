# Simulator Preview Placeholder Slot

Store optional x86_64 simulator preview payloads here when A-thread provides them.

Expected naming:

- `preview.x86_64.<type>.<mode>.<scenario>.json`

If the folder contains no matching JSON files, simulator placeholder validation reports `skipped-no-x86-placeholder`.
